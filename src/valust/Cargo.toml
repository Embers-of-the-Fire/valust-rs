[package]
name = "valust"
version = "0.1.0"
edition = "2021"

[dependencies]
valust-derive = { workspace = true, optional = true }

[features]
default = ["derive"]
derive = ["valust-derive"]

[lints]
workspace = true
